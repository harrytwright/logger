language: node_js

os:
  - linux
  - osx

env:
  - SKIP_DTRACE=1

node_js:
  - 'node'


# Gives us faster boot time, see https://docs.travis-ci.com/user/ci-environment/
sudo: false

script:
  - npm run-script build
